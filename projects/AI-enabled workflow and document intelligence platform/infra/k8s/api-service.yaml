apiVersion: v1
kind: Service
metadata:
  name: ai-workflow-api
  namespace: ai-workflow
spec:
  selector:
    app: ai-workflow-api
  ports:
    - name: http
      port: 80
      targetPort: 8101
  type: ClusterIP

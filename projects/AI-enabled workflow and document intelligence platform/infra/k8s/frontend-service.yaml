apiVersion: v1
kind: Service
metadata:
  name: ai-workflow-frontend
  namespace: ai-workflow
spec:
  selector:
    app: ai-workflow-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
